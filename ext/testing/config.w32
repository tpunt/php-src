// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("testing", "for testing support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("testing", "enable testing support", "no");

if (PHP_TESTING != "no") {
	EXTENSION("testing", "testing.c", PHP_EXTNAME_SHARED, "/DZEND_ENABLE_STATIC_TSRMLS_CACHE=1");
}

